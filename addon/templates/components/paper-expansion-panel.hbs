{{yield (hash
  collapsed=(component "paper-expansion-panel/collapsed" isExpanded=isExpanded onExpand=(action expand))
  expanded=(component "paper-expansion-panel/expanded" isExpanded=isExpanded onClose=(action collapse))
  collapse=(action collapse)
)}}

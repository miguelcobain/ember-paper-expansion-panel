{{yield (hash
  collapsed=(component "paper-expansion-panel/collapsed" isOpen=isOpen onExpand=(action expand))
  expanded=(component "paper-expansion-panel/expanded" isOpen=isOpen onClose=(action collapse))
  collapse=(action collapse)
)}}
